# Workspace Instructions

- 항상 `docs/` 디렉토리 내의 `.agent` 파일 내용인 에이전트 지시사항을 준수한다.
- 작업이 완료되면 별도 중단 지시가 없는 한 `commit`, `push`, `PR 생성`, `merge`까지 순차적으로 진행한다.
- 브랜치 보호 규칙, 리뷰 요구사항, 권한 문제 등으로 즉시 `push`/`PR`/`merge`가 불가능하면 그 지점까지 진행한 뒤 정확한 차단 사유를 보고한다.
